apiVersion: v1
kind: Service
metadata:
  name: argocd-demo-service
spec:
  type: NodePort
  selector:
    app: argocd-demo
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30007
    - port: 3000
